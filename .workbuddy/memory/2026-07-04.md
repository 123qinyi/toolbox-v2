# 2026-07-04 工作日志

## 文本处理转化工具 - Tab 响应式优化
- 问题：文本处理转化工具的 Tab 文字在手机端没有隐藏，与其他工具不一致
- 修复：给 3 个 TabsTrigger 的文字加 `hidden sm:inline`，小屏只显图标
- 已构建部署

## 工具箱工具顺序调整
- 工具顺序：客服质量分析 → KPI完成度分析 → TOP数据分析 → 文本处理转化
- KPI 工具名称加「分析」后缀
- 已构建部署

## 环比分析颜色逻辑修复（方案B：语义化）
- 变好=红色，变差=绿色
- 差评数上升=变差=绿色（与其他指标逻辑统一）
- 满意率/首响率上升=变好=红色
- 已构建部署

## 环比分析持平状态显示
- change=0 时显示灰色「持平」，不再显示「0.00%↓」误导
- 已构建部署

## 文本处理转化工具 - 重复生成功能
- 功能：批量分配处理人，导入人员名单+输入总处理量，自动均匀分配
- 三个导入按钮：A组/B组/全部（剔除组长和已离职）
- 分配逻辑：总处理量÷人数=基数，余数从前到后各+1
- 输出按人分组排列，带复制按钮和条数统计
- 已构建部署

## 客服人员管理 - 弹窗高度优化
- 问题：人员表滚动区域被 `max-h-[32vh]` 硬限死，弹窗高度增大也没用
- 修复：`max-h-[32vh]` → `max-h-[60vh]`，弹窗宽度 720→960px
- 教训：只调外层容器不够，要排查内层是否有硬编码的高度限制
- 排序优先级：组别（A>B）→ 类型（组长>VIP>基础）→ 状态（已转正>试用期>已离职）
- 用 useMemo 排序后渲染，不影响原始数据
- 复制功能同步使用排序后列表
- DEFAULT_STAFF 数组重排为排序顺序，代码与显示一致
- 已构建部署

## 文本处理转化工具 - 格式转化功能
- 功能：| 分隔的单列文本 → Tab 分隔的多列文本，可粘贴到 Excel
- 布局：左右双栏（左输入右输出），实时转化
- 输入框带清空按钮，输出框带复制按钮（复制成功2秒后恢复）
- 已构建部署

## KPI工具三个页签标题删除 + 帮助按钮重新布局
- DataImport.tsx：删除「KPI数据处理」标题栏，帮助Dialog移到「加载示例」按钮后
- IndicatorConfig.tsx：删除「指标配置」标题栏，帮助按钮放导出配置左侧，月度季度开关放操作按钮行最右（ml-auto），清理未使用的 Settings import
- HistoryRecords.tsx：删除两处「历史记录」标题（空状态+主内容区），保留记录数和DataBackup
- 已构建部署

## TOP工具 + KPI工具 Card包裹统一
- TopTool 数据处理页：左右两栏分别用 Card 包裹（标题+按钮在 CardHeader，内容在 CardContent）
- TopTool renderResultPanel：从返回 div 改为返回 fragment（CardHeader+CardContent），适配外层 Card
- TopTool 结果分析页：右侧 renderResultPanel 外层加 Card 包裹
- DataImport 按钮区：从裸 div 改为 Card>CardContent 包裹，与客服质量分析一致
- 构建成功，部署服务暂时不可用（fetch failed），需重试
- 以 KpiTool/TopTool 为标准，统一 ServiceQualityTool 和 TextTool
- 外层div统一 `w-full`，Tabs 加 `className="w-full"`
- TabsTrigger 统一 `flex items-center gap-2`（TextTool 去掉多余的 justify-center px-3）
- TabsContent 统一 `mt-0`（去掉 mt-4 额外间距）
- ServiceQualityTool 内部有多兄弟元素，保留 `space-y-4 mt-0`
- TextTool 内部单 Card 子元素，只需 `mt-0`
- 已构建部署
- 根因：各工具TabsList各自加 max-w-2xl/mb-6/mb-4，导致宽度不铺满、间距不一致
- 修复：
  - tabs.tsx 父组件默认 gap-2 → gap-4（统一页签到内容的间距）
  - ServiceQualityTool：删 TabsList 的 max-w-2xl
  - TextTool：删 TabsList 的 max-w-2xl
  - KpiTool：删 TabsList 的 mb-6
  - TopTool：删 TabsList 的 mb-4
- 教训：样式不统一时先查共享组件的默认值，再看各工具各自加了什么覆盖
- 已构建部署
