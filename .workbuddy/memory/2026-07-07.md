# 2026-07-07 工作日志

## Git 仓库初始化 + 推送到 GitHub
- 配置 git: user.name=123qinyi, user.email=sunzeqin@enjoymi.com
- 配置 credential.helper: 先设 manager（沙盒不支持交互认证），改用 store
- git init + add . + commit "初始提交：toolbox-v2 数据工具箱"（91 files, 18863 insertions）
- git remote add origin https://github.com/123qinyi/toolbox-v2.git
- 首次 push 用 PAT token 认证成功
- token 已存储在 ~/.git-credentials，后续 push 不需要再认证
- remote URL 已清理掉 token（安全），credential.helper=store 负责后续认证

## 部署迁移：CloudStudio → EdgeOne Pages
- 原因：CloudStudio 沙盒出现 504，不稳定，且 URL 难记
- 方案：GitHub + EdgeOne Pages Git 自动部署
- 用户选择了路径 B（Git 自动部署），更新代码频繁
- EdgeOne Pages 支持 GitHub 和 Gitee
- 用户 Gitee 打不开，改用 GitHub
- GitHub 仓库已创建并推送：https://github.com/123qinyi/toolbox-v2.git
- 下一步：指导用户在 EdgeOne 控制台连接 GitHub 仓库，配置自动部署

## 部署方案变更：EdgeOne → GitHub Pages
- EdgeOne 控制台连不上（公司网络限制 console.tencentcloud.com）
- Vercel API 也连不上（api.vercel.com 超时），已注册账号但无法部署
- 最终方案：GitHub Pages（gh-pages 分支方式，绕过 workflow scope 限制）
- gh-pages 分支已推送到 GitHub（仅含 dist 产物：index.html + assets/）
- 待用户在网络稳定时手动启用：Settings → Pages → Source 选 gh-pages 分支
- GitHub Pages 预期地址：https://123qinyi.github.io/toolbox-v2/
- 同时用 CodeBuddy sandbox 部署作为临时方案（地址不变）

## 网络环境记录
- 用户在家用公司笔记本，公司网络限制多个外部服务
- 受限：api.github.com、api.vercel.com、console.tencentcloud.com
- 可用：github.com（不稳定）、pages.cloudflare.com、app.netlify.com、百度
- git push 可用（走 github.com），但 GitHub REST API 不可用（走 api.github.com）

## GitHub Actions workflow 部署推进
- token 权限已升级（加了 workflow scope），deploy.yml 已 push 到 main 分支
- 仓库已从 private 改为 public（GitHub Pages 私有仓库需付费）
- 待用户在 Settings → Pages → Source 选 "GitHub Actions" 启用
- 但 GitHub 网页反复不稳定，用户无法完成最后一步
- gh-pages 分支也推了，两种方式都准备好，等网络稳定时用户手动启用其一

## CodeBuddy sandbox 重新部署
- sandbox URL 服务端正常（curl 200，返回正确 HTML）
- 用户浏览器打不开，疑似 DNS 缓存问题
- 建议用户用无痕窗口或 flushdns 清缓存
- 地址：https://1b0f5a5bac3f49e6a03f93abdf188997.app.codebuddy.work

## GitHub Pages 部署成功！
- 用户成功在 Settings → Pages → Source 选了 "GitHub Actions"
- 首次 workflow 失败：`.gitignore` 排除了 `package-lock.json`，导致 `npm ci` 报错
- 修复：从 `.gitignore` 移除 `package-lock.json`，提交并推送
- 修复后 workflow 重新触发，约 2 分钟后部署成功
- **最终线上地址：https://123qinyi.github.io/toolbox-v2/**（HTTP 200，内容正确）
- 以后更新代码：push main → GitHub Actions 自动 build + deploy，无需手动操作
- 仓库已设为 public（GitHub Pages 免费版要求）

## 部署流程优化（4项修复）
1. **隐私修复**：`.workbuddy/` 6个记忆文件误提交到 public 仓库，已从 git 移除（`git rm -r --cached`），加入 `.gitignore`，本地文件保留
2. **跨平台兼容**：`clean:vite` 脚本的 `taskkill` 加了 `process.platform === 'win32'` 判断，Linux CI 不再执行 Windows 命令
3. **删除废弃分支**：`gh-pages` 分支（本地+远程）已删除，改用 GitHub Actions 后不再需要
4. **加固**：deploy.yml 加 `touch dist/.nojekyll`（禁用 Jekyll），package.json 加 `engines.node >= 22`
- 验证：push 后 GitHub Actions 自动重新部署，线上 200 正常

## 记忆文件纳入 Git 跟踪（换电脑恢复方案）
- 用户确认公开仓库可见记忆内容无妨
- 从 `.gitignore` 移除 `.workbuddy/` 排除规则
- `.workbuddy/memory/` 全部纳入 git 跟踪（8 个文件）
- 新增 `USER_MEMORY_BACKUP.md`：用户级记忆（~/.workbuddy/MEMORY.md）的副本
- 换电脑恢复流程：clone 仓库 → 将 USER_MEMORY_BACKUP.md 内容恢复到新电脑 ~/.workbuddy/MEMORY.md

## 按钮样式统一优化
- 标准（参照 KPI 工具 DataImport.tsx）：
  - 主操作：绿底 `bg-green-600 hover:bg-green-700` + Play 图标(w-4 h-4 mr-1) + 文字"开始处理"
  - 清空：`variant="destructive"` + Trash2 图标(w-4 h-4 mr-1) + 文字"一键清空"
- 改动文件：
  1. ServiceQualityTool.tsx："处理数据"→"开始处理"(加绿底)，删除"清空输入"按钮(合并到一键清空)，"一键清空所有"→"一键清空"，mr-2→mr-1
  2. TopTool.tsx："一键处理"→"开始处理"，"清空"→"一键清空"，RotateCcw→Trash2，w-3.5→w-4
  3. TextTool.tsx：格式化tab的"清空"按钮 ghost+Eraser → destructive+Trash2，文字"清空"→"一键清空"；Eraser 从 import 移除，换成 Trash2
- KPI 工具(DataImport.tsx)不改，已是标准
- 文本处理工具不加"开始处理"按钮（实时转换无处理动作），只统一清空按钮样式
