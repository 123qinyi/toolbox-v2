# 2026-07-17 工作日志

## 客服质量分析工具 - 新格式（会话明细）邀评指标功能完成

### 改动内容
- **EmployeeQuality 类型**新增3个字段：`inviteCount`（主动邀评数）、`inviteRate`（主动邀评率%）、`inviteSatisfactionRatio`（邀评满意转化比）
- **parseSessionData 函数**重写：解析"客服首次响应时长"列计算首响60秒应答率，解析"客服是否邀评"列计算邀评指标
  - 新格式7列输入：会话开始时间、客服首次响应时长、接待客服、访客用户名、满意度、邀评来源、客服是否邀评
  - 邀评率 = 邀评数 / 总会话数 * 100
  - 邀评满意转化比 = 满意数 / 邀评数
- **表格表头+单元格**：新增3列（主动邀评数、主动邀评率、邀评满意转化比），支持排序
- **导出逻辑**（handleCopyResult + handleExportResult）：表头、数据行、组长汇总行、总计行均追加3列
- **textarea placeholder**：更新为新7列格式
- **历史记录加载**：添加 `?? 0` 兜底，兼容旧数据
- **旧格式路径**：合并数据时补 `inviteCount: 0, inviteRate: 0, inviteSatisfactionRatio: 0` 默认值

### 部署状态
- tsc 类型检查通过
- vite build 成功
- 已 push 到 GitHub main，GitHub Actions 自动部署中
